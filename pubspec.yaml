name: project_pm
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  lottie: ^2.7.0       # untuk animasi splash screen
  fluttertoast: ^8.2.4 # untuk toast error / sukses
  google_fonts: ^6.2.1 # ✅ buat pake font Nunito

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/animations/todo.json # daftar file animasi Lottie
